//ARRAYS - 11 - Joining Arrays.
const numbers = [1, 2, 3, 4];

const joined = numbers.join('-');
console.log(joined);

const message = 'This is my first message';

const parts = message.split(' ');
console.log(parts);

const join = parts.join(' ').toUpperCase();

console.log(join);